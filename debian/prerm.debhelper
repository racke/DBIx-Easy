# Automatically added by dh_installdocs
if [ \( "$1" = "upgrade" -o "$1" = "remove" \) -a -L /usr/doc/libdbix-easy-perl0 ]; then
	rm -f /usr/doc/libdbix-easy-perl0
fi
# End automatically added section
