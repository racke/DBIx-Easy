# Automatically added by dh_installdocs
if [ "$1" = "configure" ]; then
	if [ -d /usr/doc -a ! -e /usr/doc/libdbix-easy-perl -a -d /usr/share/doc/libdbix-easy-perl ]; then
		ln -sf ../share/doc/libdbix-easy-perl /usr/doc/libdbix-easy-perl
	fi
fi
# End automatically added section
